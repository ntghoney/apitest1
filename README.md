###接口自动化测试框架
 1. 环境：
    + python3.7
    + mysql 5.7.24
 2. 依赖库
    + requests
    ```
    pip install requests
    ```
    + xlwd
     ```
      pip install xlwd
     ```
    + xlwt
    ``` 
    pip install xlwt
    ```
    + pymysql
    ``` 
    pip install pymysql
    ```